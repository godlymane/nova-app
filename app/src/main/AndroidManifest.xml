<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== TIER 1 PERMISSIONS ========== -->

    <!-- Phone & Calls -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera & Audio -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Media access (for sharing gallery photos to Instagram stories, etc.) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ========== TIER 2 PERMISSIONS ========== -->

    <!-- WiFi -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Bluetooth (API 31+ requires BLUETOOTH_CONNECT for direct toggle) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Screen brightness (WRITE_SETTINGS requested via intent at runtime) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Do Not Disturb (ACCESS_NOTIFICATION_POLICY requested via intent at runtime) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- ========== NOVA BRAIN PERMISSIONS ========== -->

    <!-- Calendar -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Location (foreground service type) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- App usage stats (requires user to grant via Settings > Special app access) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground service type for ContextEngine -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- ========== VISION / SCREENSHOT PERMISSION ========== -->

    <!-- MediaProjection for screenshot capture (vision fallback in AgentExecutor) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- ========== OVERLAY PERMISSION ========== -->

    <!-- System overlay for aura effect on top of all apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ========== CORE SYSTEM PERMISSIONS ========== -->

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Vibrate -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot completed -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required for setTimer and setAlarm intents -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- ========== WAKE WORD PERMISSIONS ========== -->

    <!-- Required for WakeWordService foreground service on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Battery optimization exclusion — keeps always-active services alive (OPlus/OnePlus) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Required to list installed apps on Android 11+ (openApp tool) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- ========== TIER 4 PERMISSIONS ========== -->
    <!-- Note: No runtime permissions needed for AccessibilityService -->
    <!-- The user enables it manually in Settings > Accessibility -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nova">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ========== WAKE WORD SERVICE ========== -->

        <!-- WakeWordService: persistent foreground service for "Hey Nova" wake word detection -->
        <!-- foregroundServiceType="microphone" required on Android 14+ (API 34) -->
        <service
            android:name=".voice.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- BootReceiver: restarts WakeWordService after device reboot -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ========== TIER 2 SERVICES ========== -->

        <!-- NotificationListenerService for readNotifications tool -->
        <service
            android:name=".services.NovaNotificationListener"
            android:label="Nova Notification Access"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ========== NOVA BRAIN SERVICES ========== -->

        <!-- Nova Brain: ContextEngine foreground service -->
        <service
            android:name=".brain.context.ContextEngine"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|specialUse"
            android:permission="android.permission.FOREGROUND_SERVICE">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Nova context collection for personalized assistance" />
        </service>

        <!-- ========== AURA OVERLAY SERVICE ========== -->

        <service
            android:name=".overlay.AuraOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Nova ambient aura visual overlay" />
        </service>

        <!-- ========== TASK BUBBLE OVERLAY SERVICE ========== -->

        <service
            android:name=".overlay.bubble.TaskBubbleService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Nova floating task progress overlay" />
        </service>

        <!-- ========== VISION SCREENSHOT SERVICE ========== -->

        <service
            android:name=".vision.ScreenshotService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- ========== TIER 4 SERVICES ========== -->

        <!-- AccessibilityService for UI automation -->
        <service
            android:name=".accessibility.NovaAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="Nova Companion"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/nova_accessibility_config" />
        </service>

        <!-- ========== HOME SCREEN WIDGET ========== -->

        <receiver
            android:name=".widget.NovaWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nova_widget_info" />
        </receiver>

    </application>

</manifest>
